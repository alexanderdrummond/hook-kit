export const hook = {
  slug: "use-copy-to-clipboard",
  title: "useCopyToClipboard",
  description: "Copies text and exposes a short-lived copied state for UI feedback.",
  category: "clipboard",
  signature: "const { copied, copy } = useCopyToClipboard(timeout)",
}

<HookSignature>{hook.signature}</HookSignature>

<HookNote>
  This matches the command-block pattern: copy an install command, show a check
  state briefly, then reset the affordance.
</HookNote>

## Code

```tsx
import * as React from "react";

export function useCopyToClipboard(timeout = 1500) {
  const [copied, setCopied] = React.useState(false);
  const timeoutRef = React.useRef<ReturnType<typeof setTimeout> | null>(null);

  const copy = React.useCallback(
    async (value: string) => {
      await navigator.clipboard.writeText(value);
      setCopied(true);

      if (timeoutRef.current) {
        clearTimeout(timeoutRef.current);
      }

      timeoutRef.current = setTimeout(() => {
        setCopied(false);
        timeoutRef.current = null;
      }, timeout);
    },
    [timeout],
  );

  React.useEffect(() => {
    return () => {
      if (timeoutRef.current) {
        clearTimeout(timeoutRef.current);
      }
    };
  }, []);

  return { copied, copy };
}
```

## Usage

```tsx
import { useCopyToClipboard } from "./use-copy-to-clipboard";

export function CopyPackageName() {
  const { copied, copy } = useCopyToClipboard();

  return (
    <button type="button" onClick={() => copy("Copied from Hook Kit")}>
      {copied ? "Copied" : "Copy text"}
    </button>
  );
}
```

## API

- `timeout` controls how long the copied state remains active.
- `copy` accepts the text that should be written to the clipboard.
- `copied` can drive icons, labels, or tooltip content.
